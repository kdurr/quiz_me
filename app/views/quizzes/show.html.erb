<section>
<div class="quiz-title">
  <h3>Take Quiz: <strong><%= @quiz.title %></strong></h3>
</div>

<div class="q_a_set">
  <%= simple_form_for([@quiz, @submission]) do |f|  %>
    <% @quiz.questions.each do |q| %>
      <strong>Question:</strong>
      <%= q.name %><br />
      <div class='answer-field'>
          <%= f.hidden_field :question, :value => q %>
          <%= f.input :result, as: :radio_buttons, 
            collection: q.question_choices, value_method: :option, 
            label_method: :option, label: false, input_html: { name: "submission[result][#{q.id}]" } %>
      </div>
    <% end %>
    <%= f.submit "Submit" %>
  <% end %>
</div>
